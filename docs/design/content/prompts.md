# AI Design Prompt Templates

Pre-configured prompts for generating design-compliant code.

## Component Generation

"Create a new React component named [Name] using the design system in `docs/design/foundations/tokens.json`. Use the `docs/design/components/variants.md` for styling logic. Ensure the output follows the `docs/design/components/responsibilities.md` structure."

## CSS Generation

"Write a CSS module for [Name] that implements the `--color-brand-accent` and `--font-sans` variables. Ensure mobile-first responsiveness with a breakpoint at 768px as specified in `docs/design/layout/structures.md`."
